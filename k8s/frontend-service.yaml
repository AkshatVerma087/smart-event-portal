apiVersion: v1
kind: Service
metadata:
  name: eventportal-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: eventportal-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
